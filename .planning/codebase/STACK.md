# STACK

## Languages & Frameworks
- Dart (>=3.0.0 <4.0.0)
- Flutter (uses-material-design)

## State Management
- Riverpod (`flutter_riverpod` ^2.5.1) - using `StateProvider` and `StateNotifier`

## Data Storage
- Hive (`hive`, `hive_flutter`) - Local NoSQL storage
- Firebase Firestore - Cloud database

## Key Dependencies
- `google_fonts` (Syne, DM Sans)
- `shared_preferences`
- `flutter_local_notifications`
- `geolocator` (Geofencing)
- `firebase_core`, `firebase_auth`, `firebase_crashlytics`, `firebase_analytics`, `firebase_messaging`
- `local_auth` (Biometrics)
- `flutter_animate`, `fl_chart`, `vibration`, `flutter_map`, `latlong2`, `flutter_tts`, `camera`, `supabase_flutter`

## Build Tools
- `build_runner`, `hive_generator` (Code generation for Hive)
- `flutter_test`, `flutter_lints`
